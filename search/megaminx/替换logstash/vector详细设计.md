### 数据包操作API

```rust
fn get(&entry, &str) -> value;
fn index_at(array, &usize) -> value;
fn insert(entry, &str, value) -> bool;
fn append(array, value/entry) -> bool;
fn set(entry, value/entry/array) -> boool;
fn remove(entry, &key) ->bool;
```

